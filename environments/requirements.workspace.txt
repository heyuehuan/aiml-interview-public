# workspace image — trimmed tier-1 stack.
# Deliberately omits torch / tensorflow / spaCy / nltk / google-adk to keep the
# build fast. Add them once build time is no longer the constraint.
#
# This file states intent; the image installs from requirements.lock so every
# candidate gets the same versions. After ANY edit here, regenerate in
# the same PR:
#   uv pip compile requirements.workspace.txt -o requirements.lock \
#       --universal --python-version 3.12 --no-annotate
# and rebuild on deploy: docker compose up -d --build code-server jupyterlab
numpy
pandas
scipy
scikit-learn
matplotlib
seaborn
statsmodels
xgboost
lightgbm
pyarrow
duckdb
# Excel readers: a problem may hand the candidate a raw .xls workbook to parse,
# and pandas dispatches read_excel to xlrd (.xls) / openpyxl (.xlsx).
xlrd
openpyxl
jupyterlab
ipykernel
ipywidgets
openai
anthropic
httpx
requests
pydantic
python-dotenv
tqdm
pytest
ruff
plotly
fastapi
uvicorn
